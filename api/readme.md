# 接口层 (API Layer)

### 职责
本目录负责定义对外暴露的 RESTful 接口。作为系统的入口，它不处理复杂的业务逻辑，仅负责接收请求、分发任务并返回最终结果。

### 核心文件
- `route_api.py`: 
  - 接收前端传来的起点、终点、场景描述及极端需求。
  - 调用 `pipelines` 中的智能体中枢执行业务。
  - 处理异步任务状态，确保在复杂计算（LLM+高德校验）期间前端不超时。

### 注意事项
- 所有的输入参数必须通过 `schemas` 进行类型检查。
- 返回值需严格符合前后端约定的 DAG JSON 格式。